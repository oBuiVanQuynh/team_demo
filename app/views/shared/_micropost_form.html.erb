<%= form_for(@team) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    Name <%= f.text_field :name, placeholder: "Create new teams" %>
  </div>
  <%= f.submit "Create", class: "btn btn-large btn-primary" %>
<% end %>